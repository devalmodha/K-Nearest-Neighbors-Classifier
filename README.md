# K-Nearest-Neighbors-Classifier
Built a K-Nearest Neighbors (KNN) classifier using Python and Scikit-learn. Demonstrates data preprocessing, feature scaling, model training, prediction, and performance evaluation through a complete machine learning classification workflow.
